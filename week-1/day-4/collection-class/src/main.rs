use std::vec::Vec;

fn main() {
    // VECTORS
    // // creating a new vector
    // let v: Vec<i32> = Vec::new();

    // // create a new vector using the vec macro holding i32 variables infer by rust
    // let v1 = vec![1, 2, 3];

    // // updating a vector
    // let mut v = Vec::new();

    // // adds element to the vector
    // v.push(5);
    // v.push(6);
    // v.push(7);
    // v.push(8);

    // // accessing a vector
    // let v = vec![1, 2, 3, 4, 5];

    // // access via indexing
    // let thrid: i32 = &v[2];
    // println!("The thrid element is {}", thrid);

    // // access via get method
    // let thrid: Option<&i32> = v.get(2);
    // match thrid {
    //     Some(thrid) => println!("The thrid element is {}", thrid),
    //     None => println!("there is no thrid elemet"),
    // }

    // // // Iterating over vecotrs
    // // immiutable reference
    // let v = vec![100, 32, 57];
    // for in in &v {
    //     println!("{}", i);
    // }

    // // iterating over mutable reference
    // let mut v = vec![100, 32, 56];
    // for i in &mut v {
    //     *i += 50;
    // }

    // // when storing multiple variable in a vetcor one can use an enum to haold all different variables
    // // the the vecotr holds variable type of enum that way the vecotor sticks to his of just one type of variable
    // // which is of type enum
    // enum SpreadsheetCell {
    //     Int(i32),
    //     Float(f64),
    //     Text(String),
    // }

    // let row = vec![
    //     SpreadsheetCell::Int(3),
    //     SpreadsheetCell::Text(STring::from("blue")),
    //     SpreadsheetCell::Float(10.12),
    // ];

    // STRING
    // creating a new string
    let mut s = String::new();
}
